# Retrospective — S4 docs-only depth probe (T-004) and the fresh-container linter gap (T-031)

- **UTC date**: 2026-07-06 (verified via `date -u +%Y-%m-%d`, cross-checked with `python3 datetime.UTC`)
- **Last PR**: #3 (highest PR number covered by this retro)
- **Branch at write time**: claude/s4-depth-probe-triage-ttkz2r
- **Sibling artifacts**: [./2026-07-06-3/](./2026-07-06-3/)
- **Scope**: PR #3 only — the T-004 depth-probe work. Written post-merge on the designated branch restarted from main; the prior same-day retrospective `2026-07-06-2` covered PRs #1–#2, so the two tile without overlap.

## Commit hashes by PR

### PR #3 — claude/s4-depth-probe-triage-ttkz2r (merged 2026-07-06)

- `c73fd5a` Execute T-004: S4 docs-only depth probe (experiment 001)
- `48e9f6a` Add T-031: install jsonschema so the JSON linter runs in fresh sessions

## Part 1 — what happened

### Phase 1 — Orient and sanity-check the boundary

The session's charge was to execute T-004 (the S4 depth probe) exactly as the ledger specified and to stop before T-005 (triage, done with the owner). Orientation was read-only: TODO.json items T-004/T-005, the charter's docs-blindness contract, scenario-brainstorm item 4, the experiments/ README, and the canonical-json-editing skill. The docs-blindness boundary hook was sanity-checked once at the start — `scripts/hooks/test-deny-platform-source` returned 19/19 — so isolation was enforced before any subagent ran, not merely promised.

### Phase 2 — Ledger flips and the fresh-container wall

Marking T-001 done and T-004 in-progress through `scripts/jq-edit` failed on the first call: `validate-schema: the python 'jsonschema' package is not installed`, exit 2. The mutation had actually landed — the failure was the linter's schema step, not the data. A fresh remote container simply does not carry the `jsonschema` package the linter depends on. `pip install jsonschema` fixed it and `scripts/lint-json` went green. This was the session's first unplanned discovery and became T-031.

### Phase 3 — Launch and capture the docs-blind probe

One fresh general-purpose subagent was launched with a prompt containing only the role (tenant application developer), the S4 bullet verbatim, the docs root URL as sole source, the output shape, and the recording discipline (cite every step; missing facts are the product; type every assumption; record blocks and continue). It returned a full seven-step L1 with a url+heading+exact-quote citation on every step. Capturing that output verbatim turned out to be the second unplanned lesson: the completion notification HTML-escapes `<`/`>`/`"`, so the copy had to come from the transcript JSONL — and the agent's final message arrived one byte short of well-formed JSON, missing its single top-level closing brace. A small extractor dropped a one-sentence prose preamble, appended exactly one `}`, verified the result parsed and was fully consumed, and wrote `probe-output.json`. Both deviations were disclosed.

### Phase 4 — Write up the experiment

`experiments/001-s4-depth-probe/README.md` records the probe question, how the probe was built, the integrity caveats, and — the point of the exercise — the candidate-finding count next to the owner's recorded prediction. Candidates = 12 missing_facts + 1 blocked + 2 platform-fact assumptions = **15, against the predicted 5–10**, on the best-documented path in the corpus. T-004 was marked done with a delivery note; T-005 left pending.

### Phase 5 — Owner checkpoint, T-031, merge

Reporting back, the owner asked whether all findings were recorded. The docs candidates were safe (verbatim on disk, count in the ledger). The process findings were not — chiefly the jsonschema gap. The owner's instruction was decisive and worth preserving: record it "in a place it is guaranteed to be processed," and explicitly *not* in a retrospective, "there is no defined process that ever reads a retrospective after it is produced." The gap went into the ledger as T-031. PR #3 was then merged, and the designated branch restarted from the freshly-merged main for this follow-up retrospective.

### Phase 6 — Retrospective

A cross-session collision surfaced here: a prior session had already produced `2026-07-06-2` and adopted its artifacts to main (an `AGENTS.md`, ADRs 0001–0003, the `add-governed-doctype` skill). This retro therefore deduped hard against existing governance — one candidate rule ("keep the platform-source path out of tool-call text") was dropped as an exact duplicate of the adopted `AGENTS-MD-5120e01835`.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 1 (docs-blind S4 probe) |
| PRs opened / merged | 1 / 1 (#3) |
| Real-world bugs discovered + fixed | 0 code bugs; 1 tooling gap found → ledger T-031 |
| Candidate doc findings surfaced (not filed) | 15 (triage deferred to T-005) |
| Tests added (before / after) | 0 / 0 (ran existing hook self-test: 19/19) |
| Spec amendments | 0 |
| Scenarios driven / skipped | 1 probed to L1 draft / 0 |
| Files touched at major refactors | n/a |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `capture-subagent-structured-output` | SKILL-SPEC-b92a30d853 | high | subagent I/O fidelity | [./2026-07-06-3/SKILL-SPEC-b92a30d853-capture-subagent-structured-output.md](./2026-07-06-3/SKILL-SPEC-b92a30d853-capture-subagent-structured-output.md) |
| `docs-blind-probe-subagent` | SKILL-SPEC-cde5a0c17d | high | scenario authoring | [./2026-07-06-3/SKILL-SPEC-cde5a0c17d-docs-blind-probe-subagent.md](./2026-07-06-3/SKILL-SPEC-cde5a0c17d-docs-blind-probe-subagent.md) |

Both are grounded in this session and directly reusable: the capture skill on every future subagent whose output is the product, and the probe skill on the ledger's breadth sweep (T-024) and batch L1 authoring (T-029).

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory (two sections: `# agent instruction`, concatenated into `AGENTS.md` by the assembler, and `# justification`, stripped by it). This part mirrors both inline for review. All four are deduped against the rules already in `AGENTS.md`.

### Suggestion 1: Verify lint dependencies are importable in a fresh container — `AGENTS-MD-4fb3cf1ae4`

- **Per-rule file**: [./2026-07-06-3/AGENTS-MD-4fb3cf1ae4-verify-lint-dependencies-importable-in-a-fresh-container.md](./2026-07-06-3/AGENTS-MD-4fb3cf1ae4-verify-lint-dependencies-importable-in-a-fresh-container.md)

**Proposed agent instruction:**

> **Verify lint dependencies are importable in a fresh container.** Before relying on `scripts/lint-json` or `scripts/jq-edit` in a new remote session, confirm the Python `jsonschema` package imports (`python3 -c 'import jsonschema'`); if it does not, run `pip install jsonschema` first. Without it, both scripts fail with `validate-schema` exit 2 on the very first JSON mutation — schema validation cannot run — and the failure reads like a lint violation rather than a missing dependency.
>
> *Grounded in: the first jq-edit of the T-004 session failing on a fresh container until jsonschema was installed (see ledger T-031).*

**Justification:**

Executing T-004, the first `scripts/jq-edit TODO.json …` of the session failed immediately with `validate-schema: the python 'jsonschema' package is not installed` and exit 2 — the ledger mutation had actually landed, but the linter could not confirm it, and the error surface looked like a schema violation rather than an environment gap. The canonical-json-editing skill documents the dependency, but nothing in a fresh remote container installs it, so every new session hits this wall before it can touch a canonical document. The permanent fix is tracked as ledger item T-031 (deterministic provisioning); until that lands, the marginal cost of this rule is a single `import` check at session start, which turns a confusing exit-2 into a one-line install. The asymmetry is stark: one cheap check versus a mislabeled failure on the session's first mutation.

### Suggestion 2: Capture a subagent's structured output from the transcript, not the notification — `AGENTS-MD-dcff948de1`

- **Per-rule file**: [./2026-07-06-3/AGENTS-MD-dcff948de1-capture-subagent-structured-output-from-the-transcript.md](./2026-07-06-3/AGENTS-MD-dcff948de1-capture-subagent-structured-output-from-the-transcript.md)

**Proposed agent instruction:**

> **Capture a subagent's structured output from the transcript, not the notification.** When a subagent returns machine-readable output, do not copy it from the completion-notification wrapper — that wrapper HTML-escapes `<`, `>`, `&`, and `"` and is display-truncated. Read the final message verbatim from the task transcript JSONL instead. Expect a possible final-token truncation; if you must repair the text to make it parse, keep the repair to the terminal delimiter only, verify the result parses with no trailing content, and disclose the exact repair in the artifact that stores the output.
>
> *Grounded in: saving the S4 probe output, whose final message arrived one byte short of well-formed JSON.*

**Justification:**

The S4 depth-probe subagent returned a ~23 KB JSON object dense with `<`, `>`, and `"` — every one of which appears in the `<task-notification>` block as `&lt;`, `&gt;`, `&quot;`. Copying the output from that notification would have silently corrupted the evidence the whole experiment exists to produce. Worse, the agent's final message arrived one byte short of well-formed — missing its single top-level closing brace — so a naive copy would not even parse. Extracting from the transcript JSONL and repairing exactly one delimiter (then disclosing it) preserved fidelity. The marginal cost of the rule is one small extractor script; the cost of ignoring it is a corrupted or unparseable record that looks authoritative. This repository runs subagents as its primary instrument, so the rule pays for itself on the very next probe.

### Suggestion 3: Route actionable items to the ledger, never to a retrospective — `AGENTS-MD-1c1a8dfbe4`

- **Per-rule file**: [./2026-07-06-3/AGENTS-MD-1c1a8dfbe4-route-actionable-items-to-the-ledger-not-a-retrospective.md](./2026-07-06-3/AGENTS-MD-1c1a8dfbe4-route-actionable-items-to-the-ledger-not-a-retrospective.md)

**Proposed agent instruction:**

> **Route actionable items to the ledger, never to a retrospective.** Any defect, follow-up, or todo discovered mid-session goes into `TODO.json` as a `T-NNN` item via `scripts/jq-edit` — the queue future sessions actually read. Retrospectives are knowledge-harvest only: no defined process reads a retrospective after it is produced, so an actionable item parked there is lost. A retrospective may still propose skills, agents-file rules, and ADR drafts (each has a downstream consumer), but a discovered defect belongs in the ledger.
>
> *Grounded in: the owner redirecting the fresh-container jsonschema gap into ledger item T-031 rather than the retrospective.*

**Justification:**

When the session surfaced the jsonschema setup gap, the first instinct was to let the retrospective carry it. The owner corrected this directly: "retrospectives are NOT places to put defects or todo items — there is no defined process that ever reads a retrospective after it is produced." The gap was instead recorded as ledger item T-031, where the handoff discipline (already codified in AGENTS.md as "TODO.json is the handoff") guarantees a future session sees it. This rule sharpens that existing handoff rule with the specific negative: the retrospective is the wrong home for anything actionable. The marginal cost is one jq-edit; the cost of getting it wrong is a real defect evaporating into a document nobody re-reads.

### Suggestion 4: Restart the designated branch from main after its PR merges — `AGENTS-MD-96e9f82123`

- **Per-rule file**: [./2026-07-06-3/AGENTS-MD-96e9f82123-restart-the-designated-branch-from-main-after-its-pr-merges.md](./2026-07-06-3/AGENTS-MD-96e9f82123-restart-the-designated-branch-from-main-after-its-pr-merges.md)

**Proposed agent instruction:**

> **Restart the designated branch from main after its PR merges.** When the pull request for your working branch has merged, do not stack follow-up commits on the merged history. Run `git fetch origin main` then `git checkout -B <branch> origin/main`, and open a NEW pull request for the follow-up work. Reusing the merged branch or building on its old tip pollutes the next PR's diff with already-merged commits.
>
> *Grounded in: restarting the branch from main after PR #3 merged, before writing this retrospective.*

**Justification:**

This repository advances through a sequence of small, ledger-driven PRs off `main`, and this session produced two of them in a row: the T-004 depth probe (PR #3), then the retrospective as follow-up work. After PR #3 merged, continuing on the same branch tip would have carried the already-merged probe commits into the retrospective's diff, making the second PR unreviewable. Fetching main and `checkout -B`-ing the branch onto it kept each PR's diff to exactly its own work. The marginal cost is two git commands between PRs; the cost of skipping them is a follow-up PR whose diff is contaminated with a prior PR's history — a recurring hazard given the repo's many-small-PRs cadence.

## Part 4 — proposed ADRs

**None this session.** The architectural decisions in this problem space were already recorded by the prior same-day retrospective and adopted to `docs/adr/`: canonical JSON via jq-edit (0001), TODO.json as the cross-session ledger (0002), and findings-live-here-pulled-by-consumers (0003), with two further drafts (docs-blindness hook, two-tier governance) in that retro's sibling directory. This session executed a pre-designed experiment and made no *new* binding architectural choice. The closest candidate — "actionable items go to the ledger, not a retrospective" — is a corollary of ADR 0002 rather than a new decision, and is captured here as agents-rule `AGENTS-MD-1c1a8dfbe4` instead of a manufactured ADR.
